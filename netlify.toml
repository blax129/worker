[build]
  command = "node build.mjs"
  publish = "."

[context.production.environment]
  SITE_URL = "https://privatepropertygroups.com"

[[headers]]
  for = "/*"
  [headers.values]
    X-Frame-Options = "DENY"
    X-Content-Type-Options = "nosniff"
